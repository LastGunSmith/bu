@model IEnumerable<NovaYapi_Menü.Models.Sirket>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

@*<h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SirketAdi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SirketLogo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SirketSiteIcon)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SirketAdi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SirketLogo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SirketSiteIcon)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }

    </table>*@
<a href="/Admin/Contents/Create" class="btn btn-primary btn-flat btn-addon m-b-10 m-l-5"><i class="ti-plus"></i>Ürün Ekle</a>
<div class="col-lg-12">
    <div class="card alert">
        <div class="order-list-item">
            <table class="table">
                <thead>
                    <tr>
                        <th>Ürün Adı</th>
                        <th>Ürün Resmi</th>
                        <th>icon</th>
                     
                        <th>İşlemler</th>
                       
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.SirketAdi)</td>
                            <td> <img src="@item.SirketLogo" width="90" height="90" /></td>
                            <td>@Html.DisplayFor(modelItem => item.SirketSiteIcon)</td>
                            
                            <td>
                                <a class="btn btn-warning m-b-10 m-l-5" href="/Admin/Sirkets/Details/@item.Id">
                                    <i class="ti-pencil">
                                    </i>
                                    Detaylar
                                </a>
                                <a class="btn btn-primary m-b-10 m-l-5" href="/Admin/Sirkets/Edit/@item.Id">
                                    <i class="ti-settings">
                                    </i>
                                    Düzenle
                                </a>
                                <a class="btn btn-danger m-b-10 m-l-5" href="/Admin/Sirkets/Delete/@item.Id">
                                    <i class="ti-trash">
                                    </i>
                                    Sil
                                </a>

                            </td>
                           

                        </tr>
                    }


                </tbody>
            </table>
        </div>
    </div>
    <!-- /# card -->
</div>

